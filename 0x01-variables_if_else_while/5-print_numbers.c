#include <stdio.h>

/**
 * main - prints digits 1 to 9
 *
 * Description: using the main function
 * prints 1 to 9
 * Return: 0;
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
